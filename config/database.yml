default: &default
  host: localhost
  adapter: postgresql
  encoding: utf8
  pool: 5
  username: postgres
  password:
  timeout: 5000

development:
  <<: *default
  database: simple_bot_development

test:
  <<: *default
  database: simple_bot_test
